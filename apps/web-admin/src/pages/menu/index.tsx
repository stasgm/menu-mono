import Container from '@/components/container';

const Menus = () => {
  return (
    <Container>
      <h1 className="text-xl font-bold text-gray-200">Menus</h1>
    </Container>
  );
};

export default Menus;
