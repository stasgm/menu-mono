type Response {
  status: Int!
  errors: [String]
  payload: String
}

type Mutation {
  register(name: String!, password: String!): Response!
  login(name: String!,  password: String!): Response!
  refresh: Response!
}
